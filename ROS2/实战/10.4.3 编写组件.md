---
tags:
  - ROS2
  - 实战
---
⬅️ **上一节**：[10.4.2 使用组件运行组合节点](10.4.2%20使用组件运行组合节点.md)

> 📂 **配套代码**：[`10/ws/src/learn_compose/src/talker.cpp`](../../10/ws/src/learn_compose/src/talker.cpp) · [`10/ws/src/learn_compose/src/listener.cpp`](../../10/ws/src/learn_compose/src/listener.cpp) · [`10/ws/src/learn_compose/CMakeLists.txt`](../../10/ws/src/learn_compose/CMakeLists.txt)

# talker.cpp
```cpp
#include "rclcpp_components/register_node_macro.hpp"
RCLCPP_COMPONENTS_REGISTER_NODE(learn_compose::Talker)
```
# listener.cpp
```cpp
#include "rclcpp_components/register_node_macro.hpp"
RCLCPP_COMPONENTS_REGISTER_NODE(learn_compose::Listener)
```
# CMakeLists.txt
```txt
find_package(rclcpp_components REQUIRED)
ament_target_dependencies(intra_process_pubsub std_msgs rclcpp rclcpp_components)

find_package(rclcpp_components REQUIRED)
add_library(talker_component SHARED src/talker.cpp)
ament_target_dependencies(talker_component "std_msgs" "rclcpp" "rclcpp_components")
rclcpp_components_register_nodes(talker_component "learn_compose::Talker")

add_library(listener_component SHARED src/listener.cpp)
ament_target_dependencies(listener_component "std_msgs" "rclcpp" "rclcpp_components")
rclcpp_components_register_nodes(listener_component "learn_compose::Listener")

install(TARGETS talker_component listener_component
  ARCHIVE DESTINATION lib
  LIBRARY DESTINATION lib
  RUNTIME DESTINATION bin
)
```
# 查看
```bash
ros2 component types | grep learn
```
# 发布
```bash
ros2 component load /ComponentManager learn_compose learn_compose::Talker -e use_intra_process_comms:=true
ros2 component load /ComponentManager learn_compose learn_compose::Listener -e use_intra_process_comms:=true
```

---

➡️ **下一节**：[10.5.1 同步传感器数据](10.5.1%20同步传感器数据.md)　·　📑 [返回笔记索引](../../README.md#笔记索引)
