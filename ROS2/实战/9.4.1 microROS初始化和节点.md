---
tags:
  - ROS2
  - 实战
---
⬅️ **上一节**：[9.3 PID控制，运动学正逆解，里程计计算](9.3%20PID控制，运动学正逆解，里程计计算.md)

> 📂 **配套代码**：[`999/example_project/platformio.ini`](../../999/example_project/platformio.ini) · [`999/example_project/src/main.cpp`](../../999/example_project/src/main.cpp)

## 下載
```bash
git clone https://github.com/micro-ROS/micro-ROS-Agent.git -b $ROS_DISTRO
git clone https://github.com/micro-ROS/micro_ros_msgs.git -b $ROS_DISTRO
```
# 启动agent
```bash
ros2 run micro_ros_agent micro_ros_agent udp4 --port 8888
```
# platformio.ini
```ini
board_microros_transport = wifi
https://gitee.com/ohhuo/micro_ros_olatformio.git
```
# main.cpp
```cpp
// 创建任务运行microros 相当于线程
void micro_ros_task(void *arg)
{
    // 1.设置传输协议并延迟一段时间等待设置完成
    IPAddress agent_ip; 
    agent_ip.fromString("192.168.31.93"); // 设置ROS2代理的IP地址
    set_microros_wifi_transports("SZC", "SZC030906", agent_ip, 8888); // 设置WiFi传输协议，指定设备名称、代理IP和端口号
    delay(2000); // 延迟1秒等待设置完成
    // 2.初始化内存分配器
    allocator = rcl_get_default_allocator(); // 获取默认的内存分配器
    // 3.初始化支持结构体
    rclc_support_init(&support, 0, NULL, &allocator); // 初始化支持结构体，传入参数为命令行参数和内存分配器
    // 4.初始化节点
    rclc_node_init_default(&node, "szcbot_motion_control", "", &support);
    // 5.初始化执行器
    unsigned int num_handles = 0; // 设置执行器的句柄数量为0
    rclc_executor_init(&executor, &support.context, num_handles, &allocator);
    // 循环执行器
    rclc_executor_spin(&executor); // 启动执行器，开始处理ROS2消息和事件
}
void setup()
    // 创建microros任务
    xTaskCreate(micro_ros_task, "micro_ros_task", 10240, NULL, 1, NULL);
```

---

➡️ **下一节**：[9.4.2 订阅话题控制机器人](9.4.2%20订阅话题控制机器人.md)　·　📑 [返回笔记索引](../../README.md#笔记索引)
