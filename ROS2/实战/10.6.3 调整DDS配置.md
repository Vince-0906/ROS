---
tags:
  - ROS2
  - 实战
---
⬅️ **上一节**：[10.6.2 配置局域网通信](10.6.2%20配置局域网通信.md)

> 📂 **配套代码**：[`10/ws/topic_sub_limit.xml`](../../10/ws/topic_sub_limit.xml)

# topic_sub_limit.xml
```xml
<?xml version="1.0" encoding="UTF-8" ?>
<profiles xmlns="http://www.eprosima.com/XMLSchemas/fastRTPS_Profiles">

    <!-- 默认发布者配置 -->
    <publisher profile_name="default_publisher" is_default_profile="true">
        <historyMemoryPolicy>DYNAMIC</historyMemoryPolicy>
    </publisher>

    <!-- 默认订阅者配置 -->
    <subscriber profile_name="default_subscriber" is_default_profile="true">
        <historyMemoryPolicy>DYNAMIC</historyMemoryPolicy>
    </subscriber>

   <!-- 话题 chatter 的发布者配置 -->
    <publisher profile_name="/chatter">
        <historyMemoryPolicy>DYNAMIC</historyMemoryPolicy>
        <matchedSubscribersAllocation>
            <initial>0</initial>
            <maximum>1</maximum>
            <increment>1</increment>
        </matchedSubscribersAllocation>
    </publisher>
</profiles>
```
# 限制DDS
```bash
cd ROS/10/ws
export RMW_IMPLEMENTATION=rmw_fastrtps_cpp
export RMW_FASTRTPS_USE_QOS_FROM_XML=1
export FASTRTPS_DEFAULT_PROFILES_FILE=./topic_sub_limit.xml
ros2 run demo_nodes_cpp talker
```
此时打开新终端
```bash
ros2 topic echo /chatter
```
能正常显示，但是如果打开第二个就不行，因为已经限制了为1

---

➡️ **下一节**：[10.6.4 使用DDS共享内存](10.6.4%20使用DDS共享内存.md)　·　📑 [返回笔记索引](../../README.md#笔记索引)
