---
tags:
  - python
  - ROS2
---
⬅️ **上一节**：[4.6.1 使用Launch启动多个节点](../C%2B%2B/4.6.1%20使用Launch启动多个节点.md)

> 📂 **配套代码**：[`444/ws/src/demo_cpp_service/launch/demo.launch.py`](../../444/ws/src/demo_cpp_service/launch/demo.launch.py)

# 声明
```python
    action_declare_arg_background_g = launch.actions.DeclareLaunchArgument('launch_arg_bg', default_value="150")
```
# 获取参数
```python
    action_node_turtlesim_node = launch_ros.actions.Node(
            package='turtlesim',  # 包名
            executable='turtlesim_node',  # 可执行文件名
            parameters=[{'background_g': launch.substitutions.LaunchConfiguration('launch_arg_bg',default="150")}],  # 参数
            # name='patrol_server',  # 节点名
            output='screen'  # 输出到屏幕
        )
```
这里使用`parameters=[{'background_g': launch.substitutions.launch_configuration('launch_arg_bg',default=150)}]`获取参数
最后通过指定参数
```bash
ros2 launch demo_cpp_service demo.launch.py launch_arg_bg:=0
```
注意这里可以通过加`--debug`参数打印详细日志
最后记得再末尾加上
```python
    return launch.LaunchDescription([
        action_declare_arg_background_g,
        action_declare_max_speed,
        action_node_turtlesim_node,
        action_node_patrol_client,
        action_node_turtle_control,

     # actions动作
    ])
```

---

➡️ **下一节**：[4.6.3 launch使用进阶](4.6.3%20launch使用进阶.md)　·　📑 [返回笔记索引](../../README.md#笔记索引)
