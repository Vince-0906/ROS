---
tags:
  - python
  - ROS2
---
⬅️ **上一节**：[3.3.2 订阅pose实现闭环控制](../C%2B%2B/3.3.2%20订阅pose实现闭环控制.md)

> 📂 **配套代码**：[`333/topic_practice_ws/src/status_monitor/msg/SystemStatus.msg`](../../333/topic_practice_ws/src/status_monitor/msg/SystemStatus.msg) · [`333/topic_practice_ws/src/status_publisher/status_publisher/sys_status_pub.py`](../../333/topic_practice_ws/src/status_publisher/status_publisher/sys_status_pub.py)

# msg
如果依赖于其他的msg
例如我的`SystemStatus.msg`是依赖于`builtin_interfaces/msg/Time`，此时需要省略msg，即`builtin_interfaces/Time stamp`

# CMakeLists
```txt
# Cmake函数，来自于rosidl_default_genterators 用于将msg等消息接口定义文件文件转换为库或者头文件类
rosidl_generate_interfaces(${PROJECT_NAME}
  "msg/Status.msg"
  DEPENDENCIES builtin_interfaces
)
```

# package.xml
包含消息接口的功能包声明
```xml
  <member_of_group>rosidl_interface_packages</member_of_group>
```

# 构建
完成后通过
```bash
colcon build
```
即可完成构建
在PY中使用
# 代码
```python
import rclpy
from status_monitor.msg import SystemStatus
from rclpy.node import Node
import psutil
import platform

class SysStatusPub(Node):
    def __init__(self,node_name):
        super().__init__(node_name)
        self.status_publisher_ = self.create_publisher(SystemStatus, "system_status", 10)
        self.timers_ =self.create_timer(1.0,self.timer_callback)


    def timer_callback(self):
        cpu_percent =psutil.cpu_percent()
        memory_info = psutil.virtual_memory()
        net_io_counters = psutil.net_io_counters()

        msg =SystemStatus()
        msg.stamp = self.get_clock().now().to_msg()
        msg.host_name = platform.node()
        msg.cpu_percent = cpu_percent
        msg.memory_percent =memory_info.percent
        msg.memory_total =memory_info.total /1024 /1024  # 转换为 MB
        msg.memory_available =memory_info.available /1024 /1024  # 转换为 MB
        msg.net_sent = net_io_counters.bytes_sent /1024 /1024  # 转换为 MB
        msg.net_recv = net_io_counters.bytes_recv /1024 /1024  # 转换为 MB



        self.get_logger().info(f'发布:{str(msg)}')
        self.status_publisher_.publish(msg)

def main():
    rclpy.init()
    node = SysStatusPub("sys_status_pub")
    rclpy.spin(node)
    rclpy.shutdown()
```

---

➡️ **下一节**：[3.4.4 在功能包中使用QT](../C%2B%2B/3.4.4%20在功能包中使用QT.md)　·　📑 [返回笔记索引](../../README.md#笔记索引)
