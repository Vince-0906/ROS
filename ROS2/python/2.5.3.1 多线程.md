---
tags:
  - python
  - ROS2
---
⬅️ **上一节**：[2.5.2 C++新特性](../C%2B%2B/2.5.2%20C%2B%2B新特性.md)

> 📂 **配套代码**：[`222/ws/src/demo_py_pkg/demo_py_pkg/learn_thread.py`](../../222/ws/src/demo_py_pkg/demo_py_pkg/learn_thread.py)

```python
import threading
import requests

class Download:
    def download(self, url,callback_word_count):
        print(f'线程:{threading.get_ident()}开始下载:{url}')
        response =requests.get(url)
        response.encoding = 'utf-8'
        callback_word_count(url,response.text) #调用回调函数


    def start_download(self,url,callback_word_count):
        #self.download(url,callback_word_count)
        thread= threading.Thread(target=self.download,args=(url,callback_word_count))
        thread.start()

def word_count(url,reesult):
    """
    普通函数,用于回调,统计下载结果的长度,并打印前5个字符
    """
    print(f"{url}:{len(reesult)}->{reesult[:5]}")


def main():
    download =Download()
    download.start_download("url1",word_count)
    download.start_download("url2",word_count)
    download.start_download("url3",word_count)
```

---

➡️ **下一节**：[3.2.1 通过Topic创建小说](3.2.1%20通过Topic创建小说.md)　·　📑 [返回笔记索引](../../README.md#笔记索引)
