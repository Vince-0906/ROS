---
tags:
  - ROS2
  - 导航
---
⬅️ **上一节**：[7.3.7 优化导航速度和膨胀半径以及到点精度](7.3.7%20优化导航速度和膨胀半径以及到点精度.md)

> 📂 **配套代码**：[`777/ws/src/szcbot_application/szcbot_application/init_robot_pose.py`](../../777/ws/src/szcbot_application/szcbot_application/init_robot_pose.py)

# init_robot_pose.py代码
```python
from geometry_msgs.msg import PoseStamped
from nav2_simple_commander.robot_navigator import BasicNavigator
import rclpy


def main():
    rclpy.init()
    nav = BasicNavigator()
    init_pose = PoseStamped()
    init_pose.header.frame_id = "map"
    init_pose.header.stamp = nav.get_clock().now().to_msg()
    init_pose.pose.position.x = 0.0
    init_pose.pose.position.y = 0.0
    init_pose.pose.orientation.w = 1.0
    nav.setInitialPose(init_pose)
    nav.waitUntilNav2Active() # 等待导航系统激活
    rclpy.spin(nav)
    rclpy.shutdown()

```

---

➡️ **下一节**：[7.4.2 使用TF获取机器人实时位置](7.4.2%20使用TF获取机器人实时位置.md)　·　📑 [返回笔记索引](../../README.md#笔记索引)
