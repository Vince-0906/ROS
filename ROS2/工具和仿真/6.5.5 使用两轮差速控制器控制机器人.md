---
tags:
  - ROS2
  - 工具和仿真
---
⬅️ **上一节**：[6.5.4 使用力控制器控制轮子](6.5.4%20使用力控制器控制轮子.md)

> 📂 **配套代码**：[`666/ws/src/szcbot_description/config/szcbot_ros2_controller.yaml`](../../666/ws/src/szcbot_description/config/szcbot_ros2_controller.yaml) · [`666/ws/src/szcbot_description/src/twist_to_stamped.py`](../../666/ws/src/szcbot_description/src/twist_to_stamped.py) · [`666/ws/src/szcbot_description/launch/gazebo_sim.launch.py`](../../666/ws/src/szcbot_description/launch/gazebo_sim.launch.py)

# 发布力命令
```bash
ros2 topic pub /szcbot_effort_controller/commands std_msgs/msg/Float64MultiArray "{data: [0.0001, 0.0001]}"
```
# szcbot_ros2_controller.yaml配置新增
```yaml
    szcbot_diff_drive_controller:
      type: diff_drive_controller/DiffDriveController


szcbot_diff_drive_controller:
  ros__parameters:
    left_wheel_names: ["left_wheel_joint"]
    right_wheel_names: ["right_wheel_joint"]
    #轮子的半径和轮子之间的距离
    wheel_separation: 0.2
    #wheels_per_side: 1  # actually 2, but both are controlled by 1 signal
    wheel_radius: 0.0325
    #轮子半径的乘数
    wheel_separation_multiplier: 1.0
    left_wheel_radius_multiplier: 1.0
    right_wheel_radius_multiplier: 1.0
    #发布频率
    publish_rate: 50.0
    #里程计的坐标系
    odom_frame_id: odom
    #机器人底盘的坐标系
    base_frame_id: base_footprint
    #位置和速度的协方差噪声矩阵
    pose_covariance_diagonal : [0.001, 0.001, 0.0, 0.0, 0.0, 0.01]
    twist_covariance_diagonal: [0.001, 0.0, 0.0, 0.0, 0.0, 0.01]
    #是否使用开环控制
    open_loop: true
    #是否使用里程计tf发布
    enable_odom_tf: true
    #接收cmd_vel的超时时间，单位为秒
    cmd_vel_timeout: 0.5
    #publish_limited_velocity: true
    #是否使用带时间戳的速度消息
    use_stamped_vel: false
    cmd_vel_topic: /cmd_vel
```
# launch新增
```python
    action_load_diff_drive_controller = launch.actions.ExecuteProcess(
        cmd='ros2 control load_controller szcbot_diff_drive_controller --set-state active'.split(' '),
        output='screen'
    )
    
    return launch.LaunchDescription([
        launch.actions.RegisterEventHandler(
            event_handler=launch.event_handlers.OnProcessExit(
                target_action=action_load_joint_state_controller,
                on_exit=[action_load_diff_drive_controller],
            )
        )
    ])
```
# twist_to_stamped.py‎，转换为stamped消息
## 小车不走问题的根本原因
 `diff_drive_controller` 订阅的是`geometry_msgs/msg/TwistStamped`（带时间戳的速度消息），但
`teleop_twist_keyboard` 发布的是 `geometry_msgs/msg/Twist`（不带时间戳）。消息类型不匹配，所以控制器收不到命令，车就动不了。
## 怎么定位的
### 首先确认`topic`通信：
看到`/cmd_vel topic`存在，有`publisher`也有`subscriber`，数量都对得上，说明连接本身没问题。
### 检查消息类型：
用`ros2 topic info /cmd_vel`发现这个`topic`同时支持`Twist`和`TwistStamped`两种类型，这很不寻常。
### 查看控制器实际订阅的类型：
用`ros2 node info /szcbot_diff_drive_controller`，发现它订阅的是`geometry_msgs/msg/TwistStamped`。
## 验证假设：
手动发布`Twist`消息（车没动），然后发布`TwistStamped`消息（动了），这就确认了是消息类型不匹配的问题。
## 查找配置问题：
检查了配置文件`szcbot_ros2_controller.yaml`，里面有`use_stamped_vel: false`，理论上应该让控制器接收Twist类型。但我用`ros2 param dump`导出控制器的实际参数后，发现参数列表里根本没有`use_stamped_vel`这个参数。这说明新版本的`diff_drive_controller`可能不支持这个参数，或者默认行为就是要`TwistStamped`。于是有了接下来的转换脚本
```python
#!/usr/bin/env python3
"""
Twist to TwistStamped converter
将 geometry_msgs/Twist 转换为 geometry_msgs/TwistStamped
"""
import rclpy
from rclpy.node import Node
from geometry_msgs.msg import Twist, TwistStamped


class TwistToStamped(Node):
    def __init__(self):
        super().__init__('twist_to_stamped')

        # 订阅不带时间戳的 cmd_vel（来自 teleop 等）
        self.subscription = self.create_subscription(
            Twist,
            '/cmd_vel',
            self.twist_callback,
            10
        )

        # 发布带时间戳的 cmd_vel_stamped（给控制器）
        self.publisher = self.create_publisher(
            TwistStamped,
            '/cmd_vel_stamped',
            10
        )

        self.get_logger().info('Twist to TwistStamped converter started')
        self.get_logger().info('Subscribing to: /cmd_vel (Twist)')
        self.get_logger().info('Publishing to: /cmd_vel_stamped (TwistStamped)')

    def twist_callback(self, msg):
        # 创建带时间戳的消息
        stamped_msg = TwistStamped()
        stamped_msg.header.stamp = self.get_clock().now().to_msg()
        stamped_msg.header.frame_id = 'base_footprint'
        stamped_msg.twist = msg

        # 发布
        self.publisher.publish(stamped_msg)


def main(args=None):
    rclpy.init(args=args)
    node = TwistToStamped()

    try:
        rclpy.spin(node)
    except KeyboardInterrupt:
        pass
    finally:
        node.destroy_node()
        rclpy.shutdown()


if __name__ == '__main__':
    main()

```
# szcbot.ros2_control.xacro修改
```xacro
                    <!-- 将差速驱动控制器的速度命令话题重映射到 /cmd_vel_stamped -->
                    <remapping>/szcbot_diff_drive_controller/cmd_vel:=/cmd_vel_stamped</remapping>
```
最后记得在`CMakeLists.txt`里安装

---

➡️ **下一节**：[7.2.2 将地图保存为文件](../导航/7.2.2%20将地图保存为文件.md)　·　📑 [返回笔记索引](../../README.md#笔记索引)
